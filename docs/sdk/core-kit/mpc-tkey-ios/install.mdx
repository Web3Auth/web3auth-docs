---
title: "Installing MPC tKey iOS SDK"
sidebar_label: "Install"
displayed_sidebar: sdk
description: "Installing Web3Auth MPC tKey iOS SDK | Documentation - Web3Auth"
---

## Installation

#### Swift Package Manager

### Add the MPC tKey iOS SDK

1. In Xcode, with your app project open, navigate to **File > Add Packages**.

1. When prompted, add the tKey iOS SDK repository:

   ```sh
   https://github.com/tkey/tkey-ios
   ```

   From the `Dependency Rule` dropdown, select `Branch` and enter `feat/v2-tss` as the version.

1. When finished, Xcode will automatically begin resolving and downloading your dependencies in the background.

### Add the CustomAuth Swift SDK

1. In Xcode, with your app project open, navigate to **File > Add Packages**.

1. When prompted, add the CustomAuth Swift SDK repository:

   ```sh
   https://github.com/torusresearch/customauth-swift-sdk
   ```

   From the `Dependency Rule` dropdown, select `Branch` and enter `feat/sapphire` as the version.

1. When finished, Xcode will automatically begin resolving and downloading your dependencies in the background.

### Add the TSS Client Swift SDK

1. In Xcode, with your app project open, navigate to **File > Add Packages**.

1. When prompted, add the CustomAuth Swift SDK repository:

   ```sh
   https://github.com/torusresearch/tss-client-swift/
   ```

   From the `Dependency Rule` dropdown, select `Up to Next Major`.

1. When finished, Xcode will automatically begin resolving and downloading your dependencies in the background.
