---
title: "Installing Core Kit tKey Android SDK"
sidebar_label: "Install"
displayed_sidebar: sdk
description: "Web3Auth Core Kit tKey Android SDK - Install | Documentation - Web3Auth"
---

## Add Web3Auth's tKey to Gradle

In your project-level `build.gradle` or `settings.gradle` file, add JitPack repository:

```groovy
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // highlight-next-line
        maven { url "https://jitpack.io" } // <-- Add this line
    }
}
```

Then, in your app-level `build.gradle` dependencies section, add the following:

```groovy
dependencies {
    // ...
    // highlight-next-line
    implementation 'com.github.tkey:tkey-android:0.0.5'
}
```

:::info Latest-SDK

Check the **[latest version of Web3Auth's tKey Android SDK](https://github.com/tkey/tkey-android/releases)** and update accordingly.

:::

### Permissions

Open your app's `AndroidManifest.xml` file and add the following permission:

```xml
// highlight-next-line
<uses-permission android:name="android.permission.INTERNET" />
```

> Note: The `<uses-permission>` element must be a direct child of the `<manifest>` root element
