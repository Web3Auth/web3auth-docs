---
title: Web3Auth Unreal Engine SDK
displayed_sidebar: sdk
description: "Unreal Engine SDK | Documentation - Web3Auth"
image: "images/docs-meta-cards/sdk-reference-card.png"
---

import androidDeepLink from "@site/static/content-hub/sdk/unreal/android-deeplink.png";
import iOSExtraPlist from "@site/static/content-hub/sdk/unreal/ios-extraplist.png";

---

The Web3Auth [Unreal Engine SDK](https://github.com/Web3Auth/web3auth-unreal-sdk) is a client-side library which allows you to authenticate users
using Web3Auth on `Unreal` game engine. This SDK has been written majorly in `C++` and can be integrated preferably across all mobile platforms. It
returns a private key generated in a non-custodial way on successful user authentication. This authentication can be achieved by using any social
login options that Web3Auth supports or using a custom authentication flow of your choice.

### Requirements

- Unreal Engine `v5.1.1` with Xcode 14 or Unreal Engine `v5.0.3` with Xcode 13.
- [Epic Game Launcher](https://www.unrealengine.com/en-US/download) to download Unreal library.

:::tip sample app

Get started with the sample code provided in our [**Unreal example repository**](https://github.com/Web3Auth/web3auth-unity-example/tree/master).

:::

## Installation

---

### Download and Import the Unreal package

Download [Unreal Package](https://github.com/Web3Auth/web3auth-unreal-sdk/releases) from our latest release and import the package file into your
existing Unreal Engine project.

## Configuration

---

### Configure a Plug n Play project

- Go to [Developer Dashboard](https://dashboard.web3auth.io/), create or select an Web3Auth project:

- Add `{{SCHEMA}}://{YOUR_APP_PACKAGE_NAME}/auth` to **Whitelist URLs**.

- Copy the `Client ID` for usage later.

### Configure Deep Link for Android

To setup Android sdk and ndk for unreal editor. Please
[see the unreal documentation](https://docs.unrealengine.com/5.0/en-US/how-to-set-up-android-sdk-and-ndk-for-your-unreal-engine-development-environment).

- To add redirect URI into your Android app, open the `<Project-Path>/Plugins/Web3AuthSDK/Source/Web3AuthSDK_Android.xml` file.

- Find the `<androidManifestUpdates>` tag and inside that, will be a `<data>` tag element. Replace the exisitng redirect URI with one that you have
  registered on your Web3Auth Dashboard.

<img src={androidDeepLink} style={{ alignSelf: "center", maxWidth: "100%" }} alt="android deep link" />

### Configure Deep Link for iOS

To setup iOS for unreal editor. Please
[see the unreal documentation](https://docs.unrealengine.com/5.0/en-US/setting-up-an-unreal-engine-project-for-ios/).

To add redirect URI in your iOS configuration, you just have to add the redirect URI schema.

i.e. if `web3authapp://com.web3auth.Web3AuthUnreal` is your registered redirect URI then the schema is `web3authapp`.

- Update the following xml object and replace the {schema} with your own redirectURI schema.

```plaintext
<key>CFBundleURLTypes</key>
<array>
	<dict>
		<key>CFBundleURLName</key>
		<string>{schema}</string>
		<key>CFBundleURLSchemes</key>
		<array>
			<string>{schema}</string>
		</array>
	</dict>
</array>
```

- Paste the above string with updated schema into `Edit → Project Settings → IOS → Extra Plist Data`

<img src={iOSExtraPlist} style={{ alignSelf: "center", maxWidth: "100%" }} alt="iOS Extra Plist data" />
