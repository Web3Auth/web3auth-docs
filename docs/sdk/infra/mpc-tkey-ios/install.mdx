---
title: "Installing tKey MPC Swift SDK"
sidebar_label: "Install"

description: "Installing Web3Auth tKey MPC Swift SDK | Documentation - Web3Auth"
---

## Installation

#### Swift Package Manager

### Add the [tKey MPC Swift SDK](https://github.com/tkey/tkey-mpc-swift)

1. In Xcode, with your app project open, navigate to **File > Add Packages**.

1. When prompted, add the tKey iOS SDK repository:

   ```sh
   https://github.com/tkey/tkey-mpc-swift
   ```

   From the `Dependency Rule` dropdown, select `Branch` and type `alpha` as the branch.

1. When finished, Xcode will automatically begin resolving and downloading your dependencies in the
   background.

### Add the [CustomAuth Swift SDK](https://github.com/torusresearch/customauth-swift-sdk/)

1. In Xcode, with your app project open, navigate to **File > Add Packages**.

1. When prompted, add the CustomAuth Swift SDK repository:

   ```sh
   https://github.com/torusresearch/customauth-swift-sdk
   ```

   From the `Dependency Rule` dropdown, select `Branch` and type `alpha` as the branch.

1. When finished, Xcode will automatically begin resolving and downloading your dependencies in the
   background.

### Add the [TSS Client Swift SDK](https://github.com/torusresearch/tss-client-swift)

1. In Xcode, with your app project open, navigate to **File > Add Packages**.

1. When prompted, add the CustomAuth Swift SDK repository:

   ```sh
   https://github.com/torusresearch/tss-client-swift
   ```

   From the `Dependency Rule` dropdown, select `Exact` and enter `1.0.6` as the version.

1. When finished, Xcode will automatically begin resolving and downloading your dependencies in the
   background.
