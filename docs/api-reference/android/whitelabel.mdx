---
title: Whitelabeling
hide_table_of_contents: true
---

---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import BasicExample from "../../common/web/code/web3auth/_general-example.mdx";

## `whiteLabel`

For defining custom UI, branding, and translations for your brand app, you just need to define an additional parameter within the `Web3AuthOptions`
Object called `whiteLabel`. This parameter takes an additional object called `WhiteLabelData`.

Construct and configure the `WhiteLabelData` object as

| Parameter         | Type               | Mandatory | Description                                       |
| ----------------- | ------------------ | --------- | ------------------------------------------------- |
| `name`            | `String`           | No        | Name of your application                          |
| `logoLight`       | `String`           | No        | Light logo for dark background                    |
| `logoDark`        | `String`           | No        | Dark logo for light background                    |
| `defaultLanguage` | `String`           | No        | Default translation language to used              |
| `dark`            | `Boolean`          | No        | If true, enables dark mode. Default is light mode |
| `theme`           | `Map<String, Any>` | No        | Whitelabel theme                                  |

### Example

```kotlin
web3Auth = Web3Auth (
    Web3AuthOptions (
        context = this,
        clientId = getString (R.string.web3auth_project_id),
        network = Web3Auth.Network.MAINNET,
        redirectUrl = Uri.parse ("{YOUR_APP_PACKAGE_NAME}://auth"),
        whiteLabel = WhiteLabelData ( // Optional whitelabel object
            name = "Web3Auth Sample App",
            logoLight = null,
            logoDark = null,
            defaultLanguage = "en",
            dark = true,
            theme = hashMapOf (
                "primary" to "#123456"
            )
        )
    )
)
```

##### To know more in depth about how you can Whitelabel your application with Web3Auth, have a look at our [Whitelabeling Guide](/whitelabeling).
