## `Installation`:-

Install Web3Auth sdk from npm to use and configure web3auth modal. We are also installing `@web3auth/base` package to get access to common types and
interfaces for web3auth.

```shell
 npm i --save @web3auth/web3auth
 npm i --save @web3auth/base
```

<br />
