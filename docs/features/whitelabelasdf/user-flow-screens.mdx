---
title: User Flow Screens
image: "images/docs-meta-cards/documentation-card.png"

description: "User Flow Screens | Documentation - Web3Auth"
---

import AuthWhiteLabelConfig from "@site/src/common/sdk/pnp/web/_auth-whitelabel-config.mdx";
import AuthWhiteLabelExample from "@site/src/common/sdk/pnp/web/_auth-whitelabel-example.mdx";
import WhiteLabelShowcase from "@site/src/common/sdk/pnp/_whitelabel_showcase.mdx";

Web3Auth's User Flow Screens can be customized to match a dApp's branding using the `AuthAdapter`
settings, providing more options for a personalized user experience.

![Web3Auth - User Flow Screens](/images/whitelabel/user-flow-screens.gif)

## Arguments

You can pass the `whiteLabel` parameters to `AuthAdapter` with `@web3auth/modal`,
`@web3auth/no-modal` and even to Mobile & Gaming SDKs to whitelabel the Web3Auth's User Flow
Screens.

<AuthWhiteLabelConfig />

<WhiteLabelShowcase />

## Example

### Web

<AuthWhiteLabelExample />

### Android

```kotlin
web3Auth = Web3Auth (
  Web3AuthOptions (
    context = this,
    clientId = getString (R.string.web3auth_project_id),
    network = Network.MAINNET,
    redirectUrl = Uri.parse ("{YOUR_APP_PACKAGE_NAME}://auth"),
    // Optional whitelabel object
    // focus-start
    whiteLabel = WhiteLabelData (
      name = "Web3Auth Android Sample App",
      logoLight = "https://images.web3auth.io/web3auth-logo-w.svg",
      logoDark = "https://images.web3auth.io/web3auth-logo-w.svg",
      defaultLanguage = "en",
      dark = true,
      theme = hashMapOf (
        "primary" to "#229954"
      )
    )
    // focus-end
  )
)
```

### iOS

```swift
web3Auth = await Web3Auth(
  W3AInitParams(
    clientId:"YOUR_WEB3AUTH_CLIENT_ID",
    network: .testnet,
    // focus-start
    whiteLabel: W3AWhiteLabelData(
      name: "Web3Auth iOS Sample App",
      logoLight: "https://images.web3auth.io/web3auth-logo-w.svg",
      logoDark: "https://images.web3auth.io/web3auth-logo-w.svg",
      defaultLanguage: "en",
      dark: true,
      theme: ["primary": "#229954"])
    ))
    // focus-end
```

### React Native

```tsx
import * as WebBrowser from "@toruslabs/react-native-web-browser";
// or  import * as WebBrowser from "expo-web-browser"; // (for expo)

const web3auth = new Web3Auth(WebBrowser, {
  clientId:
    "BPi5PB_UiIZ-cPz1GtV5i1I2iOSOHuimiXBI0e-Oe_u6X3oVAbCiAZOTEBtTXw4tsluTITPqA8zMsfxIKMjiqNQ",
  network: Network.TESTNET,
  // focus-start
  whiteLabel: {
    name: "Web3Auth ReactNative Sample App",
    logoLight: "https://images.web3auth.io/web3auth-logo-w.svg",
    logoDark: "https://images.web3auth.io/web3auth-logo-w.svg",
    defaultLanguage: "en",
    dark: true,
    theme: {
      primary: "#229954",
    },
  },
  // focus-end
});
```

### Flutter

```dart
Future<void> initPlatformState() async {
  final themeMap = HashMap<String, String>();
  // focus-next-line
  themeMap['primary'] = "#229954";

  Uri redirectUrl;
  if (Platform.isAndroid) {
    redirectUrl = Uri.parse('{SCHEME}://{HOST}/auth');
    // w3a://com.example.w3aflutter/auth
  } else if (Platform.isIOS) {
    redirectUrl = Uri.parse('{bundleId}://openlogin');
    // com.example.w3aflutter://openlogin
  } else {
    throw UnKnownException('Unknown platform');
  }

  await Web3AuthFlutter.init(Web3AuthOptions(
    clientId: "WEB3AUTH_CLIENT_ID",
    network: Network.testnet,
    redirectUrl: redirectUrl,
    // focus-start
    whiteLabel: WhiteLabelData(
      name: "Web3Auth Flutter App",
      logoLight: "https://images.web3auth.io/web3auth-logo-w.svg",
      logoDark: "https://images.web3auth.io/web3auth-logo-w.svg",
      defaultLanguage: "en",
      dark: true,
      theme: themeMap)
    ));
    // focus-end
}
```

### Unity

```cs
void Start()
{
  web3Auth = GetComponent<Web3Auth>();
  web3Auth.setOptions(new Web3AuthOptions()
  {
    redirectUrl = new Uri("torusapp://com.torus.Web3AuthUnity/auth"),
    clientId = "BAwFgL-r7wzQKmtcdiz2uHJKNZdK7gzEf2q-m55xfzSZOw8jLOyIi4AVvvzaEQO5nv2dFLEmf9LBkF8kaq3aErg",
    network = Web3Auth.Network.TESTNET,
    // focus-start
    whiteLabel = new WhiteLabelData() {
      name = "Web3Auth Sample App",
      logoLight = "https://images.web3auth.io/web3auth-logo-w.svg",
      logoDark = "https://images.web3auth.io/web3auth-logo-w.svg",
      defaultLanguage = "en",
      dark = true,
      theme = new Dictionary < string, string > {
        {
          "primary",
          "#229954"
        }
      }
    }
    // focus-end
  });
}
```

### Unreal

![Unreal Whitelabel](/guides/sdk/unreal/whitelable.png)
