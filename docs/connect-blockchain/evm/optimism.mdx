---
title: Optimism
hide_table_of_contents: false
---

## Installation

Via Web3Auth SDK, you can interact with [Optimism](https://optimism.io/). Optimism is EVM compatible so you can directly follow the
[Ethereum installation](./ethereum).

## Configure Optimism

### Mainnet

Mainnet Block Explorer URL: https://optimistic.etherscan.io

```js
import { Web3Auth } from "@web3auth/web3auth";
import Web3 from "web3";

const web3auth = new Web3AuthCore({
  clientId: "YOUR_CLIENT_ID", // get it from Web3Auth Dashboard
  chainConfig: {
    chainNamespace: "EIP155",
    chainId: "0x10",
  },
});
await web3auth.initModal();

const web3authProvider = web3auth.connect();

const web3 = new Web3(web3authProvider); // web3auth.provider
```

### Testnet

Testnet Block Explorer URL: https://kovan-optimistic.etherscan.io

```js
import { Web3Auth } from "@web3auth/web3auth";
import Web3 from "web3";

const web3auth = new Web3AuthCore({
  clientId: "YOUR_CLIENT_ID", // get it from Web3Auth Dashboard
  chainConfig: {
    chainNamespace: "EIP155",
    chainId: "0x69",
  },
});
await web3auth.initModal();

const web3authProvider = web3auth.connect();

const web3 = new Web3(web3authProvider); // web3auth.provider
```
