```js
const openloginAdapter = new OpenloginAdapter({
  adapterSettings: {
    clientId: "YOUR-WEB3AUTH-CLIENT-ID", //Optional - Provider only if you haven't provided it in the Web3Auth Instantiation Code
    network: "testnet",
    uxMode: "popup",
    loginConfig: {
      discord: {
        name: "Name of your choice",
        verifier: "YOUR-VERIFIER-NAME-ON-WEB3AUTH-DASHBOARD",
        typeOfLogin: "discord",
        clientId: "YOUR-CLIENTID-FROM-DISCORD",
      },
    },
  },
});

web3auth.configureAdapter(openloginAdapter);
```
