Open your app's `AndroidManifest.xml` file and add the following permission. Please make sure the
`<uses-permission>` element should be a direct child of the `<manifest>` root element.

```xml
<uses-permission android:name="android.permission.INTERNET" />
```
