:::caution Requirements Android

- Android API version `24` or newer is required.
- `targetSdk` & `compileSdk` should be `32` or newer.
- Check `android/app/build.gradle` in your Flutter project to change it.

:::
