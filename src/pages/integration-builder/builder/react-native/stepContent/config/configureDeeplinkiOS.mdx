---
title: Configure Deep Link iOS
---

- Make sure that the minimum SDK compile version in `Podfile` is 13 or more.
- Install the Cocoa Pods within your project directory.
  ```
  cd ios && pod install && cd ..
  ```
