---
title: Install Expo WebBrowser
---

You will also require a WebBrowser implementation to allow our JS-based SDK to interact with the native APIs.

Install the `expo-web-browser`:

```bash
expo install expo-web-browser
```
