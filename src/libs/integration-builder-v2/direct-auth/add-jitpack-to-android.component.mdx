### Add Jitpack to your Android dependency repositories

We use Jitpack for release management, so you need to add it to your Android
project top-level `build.gradle`:

```groovy
repositories {
  maven { url "https://jitpack.io" }
}
```
