### Instantiate SDK instance

Instantiate the DirectAuth Web SDK with your configured `baseUrl` and one of two
our networks `mainnet` or `testnet`.

```javascript
const torus = new DirectWebSdk({
  baseUrl: "http://localhost:3000/serviceworker/",
  network: "testnet", // details for test net
});
await torus.init();
```
