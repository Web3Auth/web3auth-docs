### Install Direct Auth Android SDK

Add Direct Auth Android SDK to your Android app/build.gradle and sync it to
install the package:

```groovy
dependencies {
  implementation 'org.torusresearch:torus-direct-android-sdk:1.0.0'
}
```
