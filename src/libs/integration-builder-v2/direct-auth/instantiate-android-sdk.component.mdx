### Instantiate DirectAuth Android SDK

Instantiate the package with your configured redirect URI
`YOUR_APP_SCHEME://YOUR_APP_HOST/YOUR_REDIRECT_PATH`:

```java
DirectSdkArgs args = new DirectSdkArgs("YOUR CONFIGURED REDIRECT URI", TorusNetwork.TESTNET);
TorusDirectSdk torus = new TorusDirectSdk(args, this);
```
