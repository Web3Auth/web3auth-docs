---
title: Trigger user login
---

Call `openlogin.login` to trigger user login flow where it's appropriate in your application lifecycle.

```js
await openlogin.login();
```

`openlogin.login` opens a login modal for your user to authenticate using his/her prefer login method, including SSO, social logins, passswordless,
and Touch/Face ID.
