---
title: Retrieve user's private key
---

After `openlogin.init` is called, if the user already logged in, your
application will have access to the user's private key at `openlogin.privKey`:

```js
if (openlogin.privKey) {
  console.log("User logged in. Private key: " + openlogin.privKey);
}
```
