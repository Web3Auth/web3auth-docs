---
title: Instantiate the SDK
---

Instantiate the SDK by creating an instance of class `OpenloginSDK` with your project's client ID:

```javascript
const openlogin = new Openlogin({
  clientId: "YOUR PROJECT ID",
  network: "testnet", // valid values (testnet or mainnet)
});
await openlogin.init();
```
