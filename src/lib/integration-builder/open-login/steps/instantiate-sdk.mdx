---
title: Instantiate the SDK
---

Instantiate the SDK by creating an instance of class `OpenloginSDK` with your
project's client ID:

```javascript
const openlogin = new Openlogin({
  clientId: "YOUR PROJECT ID",
  iframeUrl: "http://beta.openlogin.com",
});
await openlogin.init();
```
