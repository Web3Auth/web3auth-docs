---
title: Create the Web3Auth instance
---

Lets now create an instance of the `Web3Auth` class inside on file load.

We need `chainNamespace` to initialize web3auth class, `chainNamespace` signifies the type of chain you want to initialize web3auth with.
