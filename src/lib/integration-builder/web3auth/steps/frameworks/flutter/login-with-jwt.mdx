---
title: Login with Custom JWT
---

Call the `login()` method on the `Web3AuthFlutter` instance to trigger the login flow. You should pass `.JWT` to the `loginProvider` parameter.

Also, pass the JWT `idToken` you obtained to the `id_token` field of the `extraLoginOptions`.
