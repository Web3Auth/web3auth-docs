---
title: Trigger user login
---

Trigger user login flow when it's appropriate in your application:

```javascript
const { privateKey, publicKey, userInfo } = await torus.triggerLogin({
  typeOfLogin: "google",
  verifier: "YOUR VERIFIER",
  clientId: "YOUR GOOGLE CLIENT ID",
});
```
