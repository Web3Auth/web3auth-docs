---
title: Install CustomAuth Android SDK
---

Add CustomAuth Android SDK to your Android `app/build.gradle` and re-sync the
project to install our package:

```groovy
dependencies {
  implementation 'org.torusresearch:torus-direct-android-sdk:1.0.0'
}
```
